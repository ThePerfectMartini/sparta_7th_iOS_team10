import 'package:flutter/material.dart';

class lsh extends StatelessWidget {
  const lsh({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(
        child: Text("이시형입니다."),
      ),
    );
  }
}
