import 'package:flutter/material.dart';

class lsn extends StatelessWidget {
  const lsn({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(
        child: Column(
          children: [
            Text("이상남입니다."),
            Text("테스트 추가입니다."),
          ],
        ),
      ),
    );
  }
}
